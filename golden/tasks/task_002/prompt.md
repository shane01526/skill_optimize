# Task 002：修復失敗的測試（FizzBuzz）

`fizzbuzz.py` 中的 `fizzbuzz(n: int) -> str` 應實作經典 FizzBuzz 規則：

- 能被 3 整除 → `"Fizz"`
- 能被 5 整除 → `"Buzz"`
- 同時能被 3 和 5 整除 → `"FizzBuzz"`
- 其餘 → 該數字的字串，例如 `fizzbuzz(7)` → `"7"`

目前實作有 bug，請修好讓 `test_fizzbuzz.py` 全數通過。不要修改測試檔。
