# 複製成 .env 並填入你的金鑰（.env 已被 .gitignore 排除，不會進 git）。
# 只需設定其中一個 provider 的金鑰；偵測順序：anthropic → openai → gemini。
# 可用 SKILL_REFINER_PROVIDER 強制指定，避免被順序較前的 key 搶走。

# --- Google Gemini ---
GEMINI_API_KEY=
# 強制走 Gemini（若環境還有其他 provider 的 key）：
SKILL_REFINER_PROVIDER=gemini
# 覆寫 model（預設 gemini-2.5-flash；judge 也可用 gemini-2.5-pro）：
# SKILL_REFINER_MODEL=gemini-2.5-flash

# --- 其他 provider（擇一）---
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
